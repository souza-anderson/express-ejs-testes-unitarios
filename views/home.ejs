<%- include('partials/header') %>    
    <p><%= time  %></p>
    <ul>
        <% list.forEach(item => { %>
            <li><%= item %></li>
        <% }) %>
    </ul>

    <ul>
        <% animais.forEach(item => { %>
            <li>Nome: <%= item.name %></li>
        <% }) %>
    </ul>
<%- include('partials/footer') %>